<h1>Sign up for your hat talk here!</h1>
<img src="https://images.unsplash.com/photo-1561578428-c50f678e6cbb?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1349&q=80" width="350" alt="hats">

<h2>Check the list below and input an available slot</h2>


<h2>Here are the existing talks:</h2>
<h4>Please choose a date that does not conflict</h4>


<h1><% @talks.each do |talk| %></h1>
  <% if talk.completed == true %>
      <li><input type="checkbox" checked ><%= talk.name%>, "<%= talk.topic %>", <%= talk.date %></li>
  <% else %>
      <li><input type="checkbox"><%= talk.name%>, "<%= talk.topic %>", <%= talk.date %></li>
  <% end %>

  <%= link_to talk_path(talk), method: :delete, data: { confirm: "Are you sure you want to delete '#{talk.topic}'?" } do %>
      <i class="fas fa-trash"></i>
    <% end %>

<% end %>
